# 当前功能调用检查

检查时间：2026-09-10。目标服务：`http://127.0.0.1:8848/`。

本次直接使用浏览器按钮与运行中服务的 HTTP 接口检查，没有通过演示接口返回预设成功，也没有添加员工或事件数据。检查前后员工、事件均为 0。

| 功能 | 实际结果 |
|---|---|
| 员工与审核列表、时间及在职筛选 | 接口 HTTP 200；空库展示 0 条 |
| 模板加载与字体列表 | HTTP 200，正常加载 |
| 保存模板按钮 | 页面确认保存成功，已保留当时未保存的模板草稿；原配置先行备份 |
| 模板预览 | 实际调用合成接口，生日和周年均返回可读取的 PNG；没有选择员工时保留字段占位符 |
| 扫描与生成按钮 | 任务实际提交并完成，空库返回 0 条；任务查询 HTTP 200 |
| 下载名单模板按钮 | 实际下载成功，文件只包含真实字段表头，无示例行 |
| 新增员工表单 | 能打开，姓名/部门必填验证生效；空请求被后端以 HTTP 400 拒绝 |
| 空白名单导入 | 实际上传只有表头的文件，HTTP 400，提示没有可导入资料 |
| 批量核验、停用、审核 | 无记录时按钮禁用；空核验/停用请求 HTTP 400，不伪造处理成功 |
| 飞书同步按钮 | 实际调用 HTTP 503，页面明确提示尚未在 .env 配置飞书应用凭据 |
| 系统状态 | HTTP 200；调度任务运行，DRY_RUN=true，飞书连接未就绪 |

尚不能验证：真实名单导入/保存后的完整业务、员工三项核验、生成到审核再到飞书送达的完整流程。需要使用方提供真实名单并配置飞书应用凭据及权限。

共检查 18 次实际接口请求，详细响应见 `logs/live-call-check.json`。页面演示入口、演示名单、演示图片和请求拦截已移除；当前所有页面操作直接请求后端。没有发送飞书消息。
